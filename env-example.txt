# Server Configuration
PORT=3001
FRONTEND_URL=http://localhost:8080

# Database Configuration
# Set DB_PROVIDER to 'local' for development or 'supabase' for production
DB_PROVIDER=local

# Development (Local PostgreSQL) - Required when DB_PROVIDER=local
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sessions_db
DB_USER=postgres
DB_PASSWORD=your_local_password

# Production (Supabase) - Required when DB_PROVIDER=supabase
# Get connection string from: Supabase Dashboard > Settings > Database > Connection string > URI
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# DATABASE_URL=postgresql://postgres:your_supabase_password@db.your_project_ref.supabase.co:5432/postgres

# Agora Configuration (Required)
# Get these from your Agora Console: https://console.agora.io/
AGORA_APP_ID=your_agora_app_id
AGORA_APP_CERTIFICATE=your_agora_app_certificate

# Future Providers (Optional)
# ZOOM_API_KEY=your_zoom_api_key
# ZOOM_API_SECRET=your_zoom_api_secret

# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_API_KEY=your_twilio_api_key
# TWILIO_API_SECRET=your_twilio_api_secret
