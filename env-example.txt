# Server Configuration
PORT=3001
FRONTEND_URL=http://localhost:8080

# Database Configuration
# Option 1: Use connection string (for Neon, Supabase, or other cloud PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Option 2: Use individual parameters (for local PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sessions_db
DB_USER=postgres
DB_PASSWORD=your_local_password

# Agora Configuration (Required)
# Get these from your Agora Console: https://console.agora.io/
AGORA_APP_ID=your_agora_app_id
AGORA_APP_CERTIFICATE=your_agora_app_certificate

# Future Providers (Optional)
# ZOOM_API_KEY=your_zoom_api_key
# ZOOM_API_SECRET=your_zoom_api_secret

# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_API_KEY=your_twilio_api_key
# TWILIO_API_SECRET=your_twilio_api_secret
